Handling Input Data in Spring Boot..

Now that you have practiced GET and POST APIs, you know how a client can call an API to fetch or send data.
But in real applications, APIs don’t work alone — they also need to accept input data from the client.

This input data can come in different ways, such as:

Values inside the URL
Values sent as query parameters
Data sent as JSON in the request body
To handle these different inputs, Spring Boot provides three important annotations:

@PathVariable –
used when data comes from the URL path

@RequestParam –
used when data comes from query parameters

@RequestBody –
used when data comes from the request body (JSON)

Let’s see when and why to use each of these annotations — one by one.

##################################

PathVariable
@PathVariable is used when input data is part of the URL itself.
The value is written directly in the URL path and Spring Boot extracts it and passes it to the method.

When do we use it?
Use @PathVariable when:

You want to identify a specific resource
The value is mandatory
The URL clearly represents what you are accessing

Example URL:

/api/users/101
Here, 101 is a input value passed directly in the URL.
It Extracts values from the URL
Passes them to the controller method

Controller Code (Demo)

@RestController
@RequestMapping("/api/users")
public class UserController {

    @GetMapping("/{id}")
    public String getUserById(@PathVariable int id) {
        return "Fetching user with id: " + id;
    }
}
What’s happening here?

{id} → placeholder in the URL
@PathVariable int id → captures the value from the URL
Calling /api/users/101 → id becomes 101

Common use cases:

Fetching a user by ID
Deleting or updating a specific record

#############################

Worked Example - Using PathVariable for input
Step 1: Run the Application

Start your Spring Boot application
Once the app is running successfully, the API Tester page will open automatically in your browser
Step 2: Test “Get All Users” API

In the URL input field, type:
/api/users
Click the Send button

What happens:

The request is sent to the backend
The controller method without any input is triggered
All demo users are returned
Step 3: Test “Get User by ID” API

In the URL input field, type below URL one by one and Observe the response:
/api/users/0
/api/users/1
/api/users/2
Click the Send button

What happens:

The value 0/1/2 is taken directly from the URL
@PathVariable captures this value
The corresponding user is fetched and returned

################################          CODE          ............

package com.example.demo.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.ArrayList;
import java.util.List;

@RestController
@RequestMapping("/api/users")
public class UserController {

    // In-memory list of demo users
    private List<String> users = new ArrayList<>();

    // Constructor to add demo users
    public UserController() {
        users.add("Alice");
        users.add("Bob");
        users.add("Charlie");
    }

    // Get all users
    @GetMapping
    public List<String> getAllUsers() {
        return users;
    }

    // Get user by ID
    @GetMapping("/{id}")
    public String getUserById(@PathVariable int id) {
        if (id < 0 || id >= users.size()) {
            return "User not found";
        }
        return users.get(id);
    }
}

##################################

Practice Problem - Use PathVariable
Instructions:

Complete your code first.
Click on "Run" to start the server and make sure it runs correctly.
If you make any changes to the code after running, you must run the server again.
Only after running the server with the latest changes, your submission will reflect the updated solution.
Do not submit if the server is not running; otherwise, your solution will fail.
Goal : Understand how to use @PathVariable to fetch data based on values passed in the URL.

Available Endpoints
The following endpoints are already present in the application:

Get all products
GET /api/products
This API is already completed and working.

Get product by ID
GET /api/products/{id}
This API is incomplete and needs to be implemented.

Your Task :

Complete the API that fetches a product by ID using @PathVariable.
The API should return the correct product when a valid ID is provided.
It should return "Product not found" when the ID is invalid.

################################3

package com.example.demo.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.ArrayList;
import java.util.List;

@RestController
@RequestMapping("/api/products")
public class ProductController {

    // In-memory list of demo products
    private List<String> products = new ArrayList<>();

    // Constructor to add demo products
    public ProductController() {
        products.add("Laptop");
        products.add("Mobile");
        products.add("Headphones");
    }

    // Get all products
    @GetMapping
    public List<String> getAllProducts() {
        return products;
    }

    // Get product by ID
    @GetMapping("/{id}")
    public String getProductById(@PathVariable int id) {
        if (id < 0 || id >= products.size()){
        // Write your code here
           return "Product not found";
        }
        return products.get(id);
    }
}

##############################

Guess the Output
Which annotation is used in Spring Boot to extract the value from the URL in a GET API?

ANSWER = @PathVariable
@PathVariable is used to read a value directly from the URL path and pass it to the controller method.

Example:   /api/book/10
Here, 10 comes from the URL itself and is extracted using @PathVariable.

#############################

Map the Correct URL
Which URL correctly matches a GET API defined as:

@GetMapping("/api/user/{id}")

ANSWER  =  /api/user/5
{id} is part of the URL path, so the value must be passed directly in the URL

########################3

RequestParam
@RequestParam is used when input data is sent as query parameters in the URL.
The value is written after the ? symbol in the URL and Spring Boot reads it and passes it to the method.

When do we use it?
Use @RequestParam when:

The input value is NOT part of the main URL path
The value is often optional
The data is used for filtering, searching, or sorting
The URL represents an action, NOT a specific resource
Example URL:

/api/users?role=admin
Here, role is an input value passed as a query parameter
It Extracts values from query parameters
Passes them to the controller method
Controller Code (Demo)

@RestController
@RequestMapping("/api/users")
public class UserController {

    @GetMapping
    public String getUsersByRole(@RequestParam String role) {
        return "Fetching users with role: " + role;
    }
}
What’s happening here?

?role=admin → query parameter in the URL
@RequestParam String role → captures the value from the URL
Calling /api/users?role=admin → role becomes admin
use cases:

Searching users by name or role
Filtering results
Sorting data

##########################################

Worked Example - Using RequestParam for input
Step 1: Run the Application
Click on "Run" to Start your Spring Boot application
Once the app is running successfully, the API Tester page will open automatically in your browser

Step 2: Test “Add Numbers” API
In the URL input field, type:

/api/calc/add?a=10&b=5
Click the Send button

What happens:

The request is sent to the backend
The values a=10 and b=5 are passed as query parameters
@RequestParam captures both values
The addition result is returned
Step 3: Test “Subtract Numbers” API
In the URL input field, type:

/api/calc/subtract?a=10&b=5
Click the Send button

What happens:

The values are taken from the query parameters
@RequestParam reads a and b
The subtraction result is returned
Step 4: Test “Multiply Numbers” API
In the URL input field, type:

/api/calc/multiply?a=10&b=5
Click the Send button

What happens:

The request includes query parameters
@RequestParam extracts both values
The multiplication result is returned
Step 5: Test “Divide Numbers” API
In the URL input field, type:

/api/calc/divide?a=10&b=2
Click the Send button

What happens:

a and b are passed as query parameters
@RequestParam captures the values
The division result is returned
Step 6: Test Division by Zero
In the URL input field, type:

/api/calc/divide?a=10&b=0
Click the Send button

What happens:

@RequestParam captures the values
The controller checks for division by zero
An error message is returned

############################

package com.example.demo.controller;

import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/calc")
public class CalculatorController {

    @GetMapping("/add")
    public int add(@RequestParam int a, @RequestParam int b) {
        return a + b;
    }

    @GetMapping("/subtract")
    public int subtract(@RequestParam int a, @RequestParam int b) {
        return a - b;
    }

    @GetMapping("/multiply")
    public int multiply(@RequestParam int a, @RequestParam int b) {
        return a * b;
    }

    @GetMapping("/divide")
    public String divide(@RequestParam int a, @RequestParam int b) {
        if (b == 0) {
            return "Error: Division by zero is not allowed";
        }
        return String.valueOf(a / b);
    }
}

####################################

Practice Problem - Text Case Converter API..

You are building a Text Case Converter API using Spring Boot.
This API allows the user to enter an API URL and sends a GET request to the backend.

Instructions:

Complete your code first.
Click on "Run" to start the server and make sure it runs correctly.
If you make any changes to the code after running, you must run the server again.
Only after running the server with the latest changes, your submission will reflect the updated solution.
Do not submit if the server is not running; otherwise, your solution will fail.
Your Task :
Create a Spring Boot controller that:

Accepts text input from the URL query parameter
Uses @RequestParam to read the input
Converts the text to uppercase or lowercase
Returns the converted text as the response.
API Requirements

You must create two GET API endpoints:

Convert text to uppercase
GET /api/text/uppercase?text=value
Convert text to lowercase
GET /api/text/lowercase?text=value
Each API should:

Accept the input using @RequestParam
Read the value from the query parameter named text
Return the converted text as the response

#######################################


